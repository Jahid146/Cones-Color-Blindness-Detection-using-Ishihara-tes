<div class="first-image-section">
    <p class="f"><strong>There you go:</strong></p>
    <div class="input-container">
        <img src="/images/12.1.jpg" alt="first image" class="first-image">
        <input type="text" class="form-control" placeholder="Enter the number">
        <button type="submit" class="btn btn-primary">Submit</button>
    </div>
</div>

/* Add this CSS to your stylesheet */
.input-container {
    display: flex;
    align-items: center; /* Vertically center elements */
}

.input-container img {
    margin-right: 10px; /* Adjust the margin as needed to separate the image and input */
}




// Add an event listener for form submission
const imageForm = document.getElementById("imageForm");

imageForm.addEventListener("submit", function (event) {
    event.preventDefault(); // Prevent the form from submitting in the traditional way

    // Get the src attribute of the image
    const imageSrc = document.querySelector(".first-image").src;

    // Extract the two-digit number from the image filename
    const match = imageSrc.match(/\/images\/(\d{2})\./); // This regular expression matches "/images/" followed by two digits

    if (match) {
        const number = match[1]; // Extract the two-digit number

        // Use the extracted number (e.g., change the image based on the number)
        console.log("Extracted number:", number);

        // Implement your logic to change the image based on the extracted number here
    }
});

